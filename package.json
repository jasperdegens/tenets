{
	"name": "@tenets/skills",
	"license": "MIT",
	"type": "module",
	"workspaces": [
		"packages/*"
	],
	"scripts": {
		"test": "vitest run",
		"typecheck": "tsc -p tsconfig.json",
		"build": "npm run build --workspaces",
		"bench": "vitest bench --run"
	},
	"devDependencies": {
		"@types/node": "^24.13.3",
		"typescript": "^5.6.0",
		"vitest": "^4.0.0",
		"zod": "^4.6.5"
	},
	"version": "0.6.0",
	"description": "The tenets engineering ruleset as an agent skill: 15 rules, routing index, project-guide template, and setup commands.",
	"files": [
		"skills",
		"README.md"
	],
	"repository": {
		"type": "git",
		"url": "git+https://github.com/BarakChamo/tenets.git"
	},
	"publishConfig": {
		"access": "public"
	}
}