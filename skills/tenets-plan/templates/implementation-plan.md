# Implementation plan (fallback template)

<!-- Fallback only: when the project guide names an implementation-plan template, that one wins.
     One plan per non-trivial change (Rules 1.4, 14.1). Delete these comments when populating. -->

## Requirement

<!-- The product requirement in one or two sentences — not the mechanism (Rule 14.2). -->

## Boundary and contract

<!-- The public API, route, or command touched; inputs, outputs, typed error cases, invariants. -->

## Examples

<!-- One per business rule plus the boundary cases where it bends (Rule 14.3). Each line pairs the
     example with the test name and path it becomes. -->

## Slices

<!-- Independently green, revertable commits in order (Rule 14.4). Each names its files, the test
     that goes red first, the gate command, and anything in it that cannot run in the remote
     environment (Rule 17.4). -->

## Docs to update

<!-- READMEs, topic pages, glossary entries, decision records affected (Rule 5.5). -->
